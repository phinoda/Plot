node_modules
dist
dist-ssr
*.local

.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript incremental build cache
*.tsbuildinfo

# Claude Code local user settings (per-user, not project)
.claude/

# User's auto-backup data file — written by the in-app backup feature.
# Contains live chrome.storage.local contents; should never be committed
# (and would otherwise churn git status on every change).
plot-backup.json
